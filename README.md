# session11

```
  1. create database kampus_merdeka
  2. create table todos (
      id serial primary key,
      title varchar(50) not null,
      checked boolean not null
    )
  3. npm install
  
```
